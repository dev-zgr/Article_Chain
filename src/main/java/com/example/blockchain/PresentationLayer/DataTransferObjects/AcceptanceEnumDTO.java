package com.example.blockchain.PresentationLayer.DataTransferObjects;

/**
 * This enum specifies if the user has accepted the revise the article or not
 */
public enum AcceptanceEnumDTO {
    ACCEPTED,REJECTED
}
